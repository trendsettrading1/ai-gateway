# DalaCreate AI Gateway Deployment
# EXACT CORRECT VERSION for Render

services:
  # 1. AI GATEWAY (Backend API)
  - type: web
    name: dala-create-gateway
    env: node
    plan: starter
    region: ohio
    buildCommand: npm install
    startCommand: node gateway.js
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production

  # 2. DEMO WEBSITE (Frontend)
  - type: web
    name: dala-create-demo
    env: static
    plan: starter
    buildCommand: echo "Building demo site..."
    staticPublishPath: ./demo
    autoDeploy: true
