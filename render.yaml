# DalaCreate AI Gateway Deployment
# CORRECT version for Render Blueprints

services:
  # 1. AI GATEWAY (Backend API)
  - type: web
    name: dala-create-gateway
    env: node
    plan: free
    region: ohio
    buildCommand: npm install
    startCommand: node gateway.js
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production

  # 2. DEMO WEBSITE (Frontend) - FIXED: type: web, env: static
  - type: web
    name: dala-create-demo
    env: static
    plan: free
    region: ohio
    buildCommand: echo "Building demo site..."
    staticPublishPath: ./demo
    autoDeploy: true
